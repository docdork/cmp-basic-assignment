import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cmp.basic.assignment',
  appName: 'cmp-basic-assignment',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
